import { Typography } from "@mui/material";
import React from "react";

const NotAvailable = () => {
  return (
    <Typography variant="h1">
      No movies available with selected genre. please select different one
    </Typography>
  );
};

export default NotAvailable;
